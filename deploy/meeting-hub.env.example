# Copy to /etc/meeting-hub/meeting-hub.env (recommended)

# Required
MEETING_HUB_PASSWORD=CHANGE_ME_STRONG
SESSION_PASSWORD=CHANGE_ME_32+_CHARS_LONG
DATABASE_URL="file:/home/ubuntu/clawd/meeting-hub/data/prod.db"

# Cookie settings
# If serving over plain HTTP (no HTTPS reverse proxy yet), set:
MEETING_HUB_COOKIE_SECURE=0

# Optional (needed for Summarize)
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
