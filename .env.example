# Copy to .env and adjust values

# SQLite (local)
DATABASE_URL="file:./dev.db"

# Password gate for logging in
MEETING_HUB_PASSWORD="change-me"

# iron-session cookie encryption password (32+ chars recommended)
# Login password (required)
MEETING_HUB_PASSWORD="dev"
# Set to 0 when serving over http:// (localhost), otherwise cookies won't stick.
MEETING_HUB_COOKIE_SECURE=0

# Cookie/session encryption password (>= 32 chars; required)
SESSION_PASSWORD="dev_password_dev_password_dev_password_dev_password"

# OpenAI (for MSU3 summary)
OPENAI_API_KEY=""
OPENAI_MODEL="gpt-4o-mini"
